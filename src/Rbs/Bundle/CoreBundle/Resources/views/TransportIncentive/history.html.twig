<div class="modal-header portlet-title box blue">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true"></button>
    <h4 class="modal-title">District Name : {{ district.name }}</h4>
</div>
<div class="modal-body">
    <div class="row">
        <div class="col-md-12">
            {% if transportCommissionHistories is empty %}
                No Stock History Found
            {% else %}
            <table class="table table-bordered table-striped table-condensed flip-content">
                <thead class="flip-content">
                <tr>
                    <th class="numeric">Sl</th>
                    <th class="numeric">Date</th>
                    <th class="numeric">Added By</th>
                    <th class="numeric">Quantity</th>
                    <th class="numeric">Description</th>
                    <th class="numeric">Amount</th>
                </tr>
                </thead>
                <tbody>
                {% for index, history in transportCommissionHistories %}
                    <tr>
                        <td class="numeric">{{ index + 1 }}.</td>
                        <td class="numeric">{{ history.updatedAt | date('d-m-Y') }}</td>
                        <td class="numeric">{{ history.updatedBy }}</td>
                        <td class="numeric">{{ history.station.name }}</td>
                        <td class="numeric">{{ history.depo.name }}</td>
                        <td class="numeric">{{ history.amount | number_format(2, '.', ',') }}</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
            {% endif %}
        </div>
    </div>
</div>
<div class="modal-footer">
    {% if transportCommissionHistories is not empty %}
    <a class="btn blue" href="{{ path('transport_commission_history_list', {"id": district.id}) }}" target="_blank">
        More >>
    </a>
    {% endif %}
</div>

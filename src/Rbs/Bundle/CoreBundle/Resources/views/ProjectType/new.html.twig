{% extends '::base.html.twig' %}

{% block body -%}
    <div class="col-md-12">
        <div class="portlet box blue">
            <div class="portlet-title">
                <div class="caption">
                    <i class="fa fa-globe"></i>Add Project Type
                </div>
            </div>
            <div class="portlet-body">
                {{ include('_elements/_back_to_list_button.html.twig', {url: path('projecttype'), label: 'Back to List', 'icon': 'plus'}) }}
                {{ form(form) }}
            </div>
        </div>
    </div>
{% endblock %}

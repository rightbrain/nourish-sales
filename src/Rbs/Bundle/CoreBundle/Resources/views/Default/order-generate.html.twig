{% extends '::base.html.twig' %}

{% block body %}

    {{ form_start(form, {'method': 'POST', attr: { novalidate: 'novalidate'}}) }}
        {{ form(form) }}
    {{ form_end(form) }}

{% endblock %}
